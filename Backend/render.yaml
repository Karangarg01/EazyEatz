services:
  - type: web
    name: chatbot-backend
    env: python
    region: singapore  # Closest to India
    plan: free
    buildCommand:
      - cd CHATBOT_PROJECT/Backend && pip install -r requirements.txt
    startCommand:
      - cd CHATBOT_PROJECT/Backend && uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: DATABASE_URL
        value: mysql://root:czQQnVPjEscYzwtXJcUHoGmcOInfPDfy@shortline.proxy.rlwy.net:34427/railway
      - key: SECRET_KEY
        value: your-secret-key  # Change this if needed
